#include <stdio.h>

using namespace std;

int main()
{
    int x, n, i, megabytes, sum=0, output;

    scanf("%d\n", &x);
    scanf("%d\n", &n);

    for (i=1; i<=n; ++i)
    {
        scanf("%d", &megabytes);
        sum+=megabytes;
    }
    output = ((x*n)+x)-s;
    printf("%d", o);

    return 0;
}
